# apiVersion: v1
# kind: Pod
# metadata:
#   labels:
#     app.kubernetes.io/name: argocd-nginx
#   name: argocd-nginx
# spec:
#   containers:
#     - image: nginx
#       name: nginx
#       ports:
#         - containerPort: 80
